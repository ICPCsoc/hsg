#include "bits/stdc++.h"

using namespace std;

int main (void) {
    int a,b;
    cin >> a >> b;
    if (a >= 14 && b >= 140) {
        cout << "được" << endl;
        return 0;
    }
    cout << "ko được" << endl;
    return 0;
}