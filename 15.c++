#include "bits/stdc++.h"

using namespace std;

int main (void) {
    int a, b; 
    cin >> a >> b;
    if (a < b) {
        cout << b << endl;
        return 0;
    }
    cout << a << endl;
    return 0;
}