#include "bits/stdc++.h"

using namespace std;

void info (string name, string old) {
    cout << name << endl;
    cout << old << endl;
}

int main (void) {
    string a, b;
    cin >> a >> b;
    info(a,b);
    return 0;
}